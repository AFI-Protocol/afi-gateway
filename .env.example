# ============================================================================
# AFI Eliza Gateway — Environment Variables
# ============================================================================
# Copy this file to .env and fill in your actual values.
# DO NOT commit .env to version control.

# ----------------------------------------------------------------------------
# OpenAI API Key (required for ElizaOS character interactions)
# ----------------------------------------------------------------------------
OPENAI_API_KEY=sk-your-openai-api-key-here

# ----------------------------------------------------------------------------
# AFI Reactor Base URL (for AFI service integration)
# ----------------------------------------------------------------------------
AFI_REACTOR_BASE_URL=http://localhost:8080

# ----------------------------------------------------------------------------
# MongoDB Connection (AFI Eliza Gateway only)
# ----------------------------------------------------------------------------
# This MongoDB connection is for gateway-specific data ONLY:
#   - Chat/session history for Phoenix & friends
#   - Demo/dev-only documents (e.g., healthcheck collection)
#   - Future gateway-specific metadata
#
# IMPORTANT: This is NOT for TSSD vault data. The canonical TSSD vault
# lives in other AFI repos (afi-reactor, afi-infra) and uses separate
# database/collection names.
#
# Use your MongoDB Atlas connection string here:
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority

# Optional: Override the default database name (defaults to "afi_eliza")
AFI_MONGO_DB_NAME=afi_eliza

# ----------------------------------------------------------------------------
# Node Environment
# ----------------------------------------------------------------------------
NODE_ENV=development

